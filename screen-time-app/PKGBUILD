# Maintainer: Jules <jules@example.com>
pkgname=screen-time-app
pkgver=0.1.0
pkgrel=1
pkgdesc="A modern, minimalist time tracking dashboard built with Tauri and Svelte 5"
arch=('x86_64')
url="https://github.com/DEIN-BENUTZERNAME/screen-time-app"
license=('MIT')
depends=('webkit2gtk-4.1' 'gtk3' 'libappindicator-gtk3')
makedepends=('npm' 'cargo' 'nodejs')
source=("git+https://github.com/DEIN-BENUTZERNAME/screen-time-app.git")
sha256sums=('SKIP')

build() {
    cd "$srcdir/$pkgname"
    npm install
    npm run tauri build -- --bundles deb
}

package() {
    cd "$srcdir/$pkgname"

    # Extract the deb package built by Tauri
    bsdtar -xf src-tauri/target/release/bundle/deb/${pkgname}_${pkgver}_${arch}.deb -C "$pkgdir"
    bsdtar -xf "$pkgdir/data.tar.gz" -C "$pkgdir"

    # Cleanup unnecessary files
    rm "$pkgdir/data.tar.gz"
    rm "$pkgdir/control.tar.gz"
    rm "$pkgdir/debian-binary"
}
